@AGENTS.md

<!--
This file is intentionally a stub. Project instructions live in AGENTS.md only.

- Codex and other AGENTS.md-aware tools read AGENTS.md directly.
- Claude Code before 2.1.277 reads CLAUDE.md only, so the `@AGENTS.md` import above pulls it in.
- From 2.1.277, Claude Code falls back to AGENTS.md when a folder has no CLAUDE.md.
  This stub can be deleted once every contributor's Claude Code is 2.1.277 or later.

Add project instructions to AGENTS.md. Put Claude Code-specific instructions here,
under a heading below the import line, only when they are needed.

Claude Code strips this HTML comment before injecting the file into context.
-->
